"""Bundled Claude Code skill assets.

The canonical SKILL.md lives here so it travels with the installed wheel
and is locatable via :mod:`importlib.resources`. The repo also keeps a
copy at ``.claude/skills/hypothesize/SKILL.md`` for the demo repo's own
use; both files must stay in sync.
"""
